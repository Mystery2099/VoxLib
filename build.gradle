import org.jetbrains.kotlin.gradle.tasks.KotlinCompile

plugins {
	id 'base'
	id 'fabric-loom' version '1.10.5' apply false
	id 'net.minecraftforge.gradle' version '6.0.54' apply false
	id 'org.jetbrains.kotlin.jvm' version '2.3.0' apply false
	id 'me.champeau.jmh' version '0.7.3' apply false
}

allprojects {
	version = rootProject.mod_version
	group = rootProject.maven_group
}

subprojects {
	if (project.name != 'forge') {
		apply plugin: 'fabric-loom'
	} else {
		apply plugin: 'net.minecraftforge.gradle'
	}
	apply plugin: 'org.jetbrains.kotlin.jvm'

	repositories {
		mavenCentral()
	}

	dependencies {
		if (project.name != 'forge') {
			minecraft "com.mojang:minecraft:${rootProject.minecraft_version}"
			mappings loom.officialMojangMappings()
		}

		implementation("com.github.ben-manes.caffeine:caffeine:${rootProject.caffeine_version}") {
			exclude group: 'org.checkerframework', module: 'checker-qual'
			exclude group: 'com.google.errorprone', module: 'error_prone_annotations'
		}
	}

	java {
		withSourcesJar()
		toolchain.languageVersion = JavaLanguageVersion.of(17)
		sourceCompatibility = JavaVersion.VERSION_17
		targetCompatibility = JavaVersion.VERSION_17
	}

	tasks.withType(JavaCompile).configureEach {
		options.release.set(17)
	}

	tasks.withType(KotlinCompile).configureEach {
		kotlinOptions.jvmTarget = '17'
	}

	tasks.withType(ProcessResources).configureEach {
		exclude '**/*.xcf'
		exclude 'assets/voxlib/icon_curseforge.png'
	}

	tasks.named('jar') {
		from(rootProject.file('LICENSE')) {
			rename { "${it}_${rootProject.archives_base_name}" }
		}
	}
}

// Explicit root tasks also work when invoked with an absolute task path.
tasks.named('assemble') {
	dependsOn ':common:assemble', ':fabric:assemble', ':forge:assemble'
}

tasks.named('check') {
	dependsOn ':common:check', ':fabric:check', ':forge:check'
}

tasks.named('clean') {
	dependsOn ':common:clean', ':fabric:clean', ':forge:clean'
}

// Preserve the existing root commands without launching both loaders at once.
tasks.register('runClient') {
    impliesSubProjects = true
    group = 'fabric'
    description = 'Runs the Fabric client. Use :forge:runClient for Forge.'
    dependsOn ':fabric:runClient'
}

tasks.register('runServer') {
    impliesSubProjects = true
    group = 'fabric'
    description = 'Runs the Fabric server. Use :forge:runServer for Forge.'
    dependsOn ':fabric:runServer'
}
